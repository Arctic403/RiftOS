name: Build RiftOS Android APK

on:
  workflow_dispatch:
  push:
    branches: [android-apk, main]
    paths:
      - 'android/**'
      - 'src/**'
      - 'apps/**'
      - 'index.html'
      - 'styles.css'
      - '.github/workflows/riftos-android-apk.yml'

permissions:
  contents: write

concurrency:
  group: riftos-android-apk-${{ github.ref }}
  cancel-in-progress: true

jobs:
  apk:
    name: Build + publish Samsung Android APK
    runs-on: ubuntu-latest
    timeout-minutes: 30
    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          fetch-depth: 1

      - name: Reject removed remote MCP bridge source
        shell: bash
        run: |
          test ! -e services/rift-mcp-relay
          test ! -e .github/workflows/rift-mcp-relay.yml
          test ! -e src/riftbridge-system.js
          test ! -e android/app/src/main/java/com/riftos/app/RiftMcpRelayClient.kt
          test ! -e android/app/src/main/java/com/riftos/app/RiftMcpBridgeActivity.kt
          test ! -e android/app/src/main/java/com/riftos/app/RiftMcpInitProvider.kt
          test ! -e android/app/src/main/java/com/riftos/app/RiftBridgeSandbox.kt

      - name: Validate ChatGPT Web-only Rift AI source
        shell: bash
        run: |
          test -f src/riftai-workspace.js
          test -f android/app/src/main/java/com/riftos/app/RiftAiJournal.kt
          test ! -e src/rift-ai-workspace.html
          grep -Fq "[RiftAI] ChatGPT Web workspace registered" src/riftai-workspace.js
          grep -Fq "RiftMcpAppControl" android/app/src/main/assets/riftbrowser-mcp-app.js
          if grep -Eqi 'api\.openai\.com|OPENAI_API_KEY|Authorization:[[:space:]]*Bearer' src/riftai-workspace.js; then
            echo "Direct model API path leaked into Rift AI workspace" >&2
            exit 1
          fi

      - name: Set up Java 17
        uses: actions/setup-java@v5
        with:
          distribution: temurin
          java-version: '17'

      - name: Use preinstalled Android SDK
        shell: bash
        run: |
          SDK_ROOT="${ANDROID_HOME:-${ANDROID_SDK_ROOT:-/usr/local/lib/android/sdk}}"
          test -x "$SDK_ROOT/cmdline-tools/latest/bin/sdkmanager"
          echo "ANDROID_HOME=$SDK_ROOT" >> "$GITHUB_ENV"
          echo "ANDROID_SDK_ROOT=$SDK_ROOT" >> "$GITHUB_ENV"
          echo "$SDK_ROOT/cmdline-tools/latest/bin" >> "$GITHUB_PATH"

      - name: Ensure Android platform + build tools
        run: sdkmanager "platforms;android-36" "build-tools;36.0.0"

      - name: Set up cached Gradle
        uses: gradle/actions/setup-gradle@v4
        with:
          gradle-version: '9.5.0'
          cache-read-only: false
          cache-cleanup: on-success

      - name: Restore RiftOS signing identity
        env:
          RIFTOS_KEYSTORE_B64: ${{ secrets.RIFTOS_KEYSTORE_B64 }}
          RIFTOS_KEYSTORE_PASSWORD: ${{ secrets.RIFTOS_KEYSTORE_PASSWORD }}
          RIFTOS_KEY_ALIAS: ${{ secrets.RIFTOS_KEY_ALIAS }}
          RIFTOS_KEY_PASSWORD: ${{ secrets.RIFTOS_KEY_PASSWORD }}
        run: |
          if [ -n "${RIFTOS_KEYSTORE_B64:-}" ]; then
            printf '%s' "$RIFTOS_KEYSTORE_B64" | base64 --decode > "$RUNNER_TEMP/riftos-release.keystore"
            echo "RIFT_SIGN_STORE=$RUNNER_TEMP/riftos-release.keystore" >> "$GITHUB_ENV"
            echo "RIFT_SIGN_STORE_PASS=${RIFTOS_KEYSTORE_PASSWORD}" >> "$GITHUB_ENV"
            echo "RIFT_SIGN_ALIAS=${RIFTOS_KEY_ALIAS}" >> "$GITHUB_ENV"
            echo "RIFT_SIGN_KEY_PASS=${RIFTOS_KEY_PASSWORD}" >> "$GITHUB_ENV"
            echo "RIFT_SIGN_MODE=production-secret" >> "$GITHUB_ENV"
          else
            base64 --decode android/riftos-debug.keystore.b64 > "$RUNNER_TEMP/riftos-alpha.keystore"
            echo "RIFT_SIGN_STORE=$RUNNER_TEMP/riftos-alpha.keystore" >> "$GITHUB_ENV"
            echo "RIFT_SIGN_STORE_PASS=android" >> "$GITHUB_ENV"
            echo "RIFT_SIGN_ALIAS=riftosdebug" >> "$GITHUB_ENV"
            echo "RIFT_SIGN_KEY_PASS=android" >> "$GITHUB_ENV"
            echo "RIFT_SIGN_MODE=alpha-fallback" >> "$GITHUB_ENV"
          fi

      - name: Build unsigned RiftOS Android APK
        working-directory: android
        run: gradle --stacktrace --build-cache :app:assembleRelease

      - name: Align and sign Android 8+
        run: |
          BUILD_TOOLS="$ANDROID_HOME/build-tools/36.0.0"
          UNSIGNED="android/app/build/outputs/apk/release/app-release-unsigned.apk"
          ALIGNED="$RUNNER_TEMP/RiftOS-Android-aligned.apk"
          FINAL="$GITHUB_WORKSPACE/RiftOS-Android-debug.apk"
          test -f "$UNSIGNED"
          "$BUILD_TOOLS/zipalign" -p -f 4 "$UNSIGNED" "$ALIGNED"
          "$BUILD_TOOLS/apksigner" sign \
            --ks "$RIFT_SIGN_STORE" \
            --ks-key-alias "$RIFT_SIGN_ALIAS" \
            --ks-pass "pass:$RIFT_SIGN_STORE_PASS" \
            --key-pass "pass:$RIFT_SIGN_KEY_PASS" \
            --v1-signing-enabled true \
            --v2-signing-enabled true \
            --v3-signing-enabled false \
            --v4-signing-enabled false \
            --out "$FINAL" "$ALIGNED"
          "$BUILD_TOOLS/zipalign" -c -v 4 "$FINAL"

      - name: Verify Samsung-native APK
        run: |
          BUILD_TOOLS="$ANDROID_HOME/build-tools/36.0.0"
          FINAL="$GITHUB_WORKSPACE/RiftOS-Android-debug.apk"
          VERIFY_OUTPUT="$("$BUILD_TOOLS/apksigner" verify --verbose --print-certs "$FINAL")"
          printf '%s\n' "$VERIFY_OUTPUT"
          printf '%s\n' "$VERIFY_OUTPUT" | grep -Fq "Verified using v2 scheme (APK Signature Scheme v2): true"
          BADGING="$("$BUILD_TOOLS/aapt" dump badging "$FINAL")"
          printf '%s\n' "$BADGING" | grep -E "package:|sdkVersion:|targetSdkVersion:"
          printf '%s\n' "$BADGING" | grep -Fq "sdkVersion:'26'"
          printf '%s\n' "$BADGING" | grep -Fq "package: name='com.riftos.app'"
          unzip -p "$FINAL" assets/www/index.html | grep -Fq "SAMSUNG ANDROID"
          unzip -p "$FINAL" assets/www/src/riftandroid-platform.js | grep -Fq "system.dump.save"
          unzip -p "$FINAL" assets/www/src/riftmcp-system.js | grep -Fq "[RiftMcp] local system app registered"
          unzip -p "$FINAL" assets/www/src/riftai-workspace.js | grep -Fq "[RiftAI] ChatGPT Web workspace registered"
          unzip -p "$FINAL" assets/www/src/riftai-workspace.js | grep -Fq "ChatGPT Web"
          unzip -p "$FINAL" assets/riftbrowser-mcp-app.js | grep -Fq "[RIFT_MCP_APP_V1]"
          unzip -p "$FINAL" assets/riftbrowser-mcp-app.js | grep -Fq "<rift_call>"
          unzip -p "$FINAL" assets/riftbrowser-mcp-app.js | grep -Fq "PERF_GUARD"
          if unzip -l "$FINAL" | grep -Fq "assets/riftbrowser-chatgpt-agent.js"; then
            echo "Removed ChatGPT DOM Rift Agent leaked back into Android APK" >&2
            exit 1
          fi
          if unzip -p "$FINAL" assets/riftbrowser-mcp-app.js | grep -Fq "RIFT_AGENT_V3"; then
            echo "Removed Agent V3 protocol leaked into Rift MCP App compatibility asset" >&2
            exit 1
          fi
          if unzip -p "$FINAL" assets/riftbrowser-mcp-app.js | grep -Fq "function scanAssistantMessages"; then
            echo "Whole-chat MCP mutation scanner leaked back into RiftBrowser" >&2
            exit 1
          fi
          if unzip -l "$FINAL" | grep -Fq "rift-ai-workspace.html"; then
            echo "Discarded second-WebView Rift AI HTML asset leaked into APK" >&2
            exit 1
          fi
          if unzip -p "$FINAL" assets/www/src/riftai-workspace.js | grep -Eqi 'api\.openai\.com|OPENAI_API_KEY|Authorization:[[:space:]]*Bearer'; then
            echo "Direct model API path leaked into packaged Rift AI workspace" >&2
            exit 1
          fi
          DEX_STRINGS="$(unzip -p "$FINAL" classes.dex | strings)"
          if printf '%s\n' "$DEX_STRINGS" | grep -Eq 'RiftMcpRelayClient|RiftMcpBridgeActivity|RiftMcpInitProvider|rift-bridge-device-v1|wss://your-relay'; then
            echo "Removed remote MCP bridge runtime leaked back into Android APK" >&2
            exit 1
          fi
          if printf '%s\n' "$DEX_STRINGS" | grep -Fq "RiftBrowserActivity"; then
            echo "Obsolete full-screen RiftBrowserActivity leaked back into Android APK" >&2
            exit 1
          fi
          if unzip -l "$FINAL" | grep -Fq "libllamaserver.so"; then
            echo "Removed local-AI runtime leaked back into Android APK" >&2
            exit 1
          fi
          if unzip -l "$FINAL" | grep -Eqi 'pwa-ios|app-safari|manifest\.webmanifest|local-test-sw'; then
            echo "Web/iOS-only assets leaked into Android APK" >&2
            exit 1
          fi
          echo "Signing mode: $RIFT_SIGN_MODE"

      - name: Upload verified APK artifact
        uses: actions/upload-artifact@v4
        with:
          name: RiftOS-android-debug
          path: RiftOS-Android-debug.apk
          if-no-files-found: error

      - name: Publish stable latest APK download
        env:
          GH_TOKEN: ${{ github.token }}
        run: |
          if gh release view android-latest >/dev/null 2>&1; then
            gh release upload android-latest RiftOS-Android-debug.apk --clobber
          else
            gh release create android-latest RiftOS-Android-debug.apk \
              --target "$GITHUB_SHA" \
              --title "RiftOS Android — Latest Debug" \
              --notes "Samsung/Android-native RiftOS alpha. Android 8.0 / API 26+."
          fi
