{
  "name": "riftos",
  "private": true,
  "type": "module",
  "scripts": {
    "check:transport": "node scripts/validate-rift-transport.mjs && node scripts/test-rift-raw-protocol.mjs",
    "check:riftdev": "npm --prefix apps/riftdev run check",
    "check": "npm run check:transport && npm run check:riftdev"
  }
}
